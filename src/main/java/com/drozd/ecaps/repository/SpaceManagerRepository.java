package com.drozd.ecaps.repository;

import com.drozd.ecaps.model.SpaceManager;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpaceManagerRepository extends JpaRepository<SpaceManager, Long> {
}